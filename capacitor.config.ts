import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'project_ionic_exemple',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;

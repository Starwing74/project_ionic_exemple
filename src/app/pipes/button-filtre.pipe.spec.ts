import { ButtonFiltrePipe } from './button-filtre.pipe';

describe('ButtonFiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonFiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
